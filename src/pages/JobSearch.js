import React, { useEffect, useState, useRef } from 'react';

const JobSearch = () => {

return (
	<div
    style={{
      display: 'flex',
      justifyContent: 'centre',
      alignItems: 'centre',
      height: '100vh'
    }}
	>
	<h1>Job Search</h1>
	</div>
);
};

export default JobSearch;
